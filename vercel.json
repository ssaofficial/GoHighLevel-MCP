{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "api/ghl.js",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 60
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/ghl.js" }
  ]
}
